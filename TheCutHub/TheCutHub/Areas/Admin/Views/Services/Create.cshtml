@model TheCutHub.Models.Service
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Add New Service";
}

<div class="container mt-5">
    <h2 class="section-title text-center mb-4">
        <i class="bi bi-scissors me-2 text-gold"></i> @ViewData["Title"]
    </h2>

    <div class="card shadow p-4 mx-auto" style="max-width: 600px;">
        <form asp-action="Create" method="post">
            @Html.AntiForgeryToken()

            <div class="mb-3">
                <label asp-for="Name" class="form-label text-gold">
                    <i class="bi bi-tag-fill me-1"></i> Name
                </label>
                <input asp-for="Name" class="form-control" placeholder="e.g. Haircut + Beard" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="Price" class="form-label text-gold">
                        <i class="bi bi-currency-dollar me-1"></i> Price
                    </label>
                    <input asp-for="Price" class="form-control" placeholder="e.g. 25.00" step="0.01" min="0" />
                    <span asp-validation-for="Price" class="text-danger"></span>
                </div>

                <div class="col-md-6 mb-3">
                    <label asp-for="DurationMinutes" class="form-label text-gold">
                        <i class="bi bi-stopwatch me-1"></i> Duration
                    </label>
                    <select asp-for="DurationMinutes" class="form-select">
                        <option value="">-- Select --</option>
                        @for (int i = 10; i <= 180; i += 5)
                        {
                            <option value="@i">@i min</option>
                        }
                    </select>
                    <span asp-validation-for="DurationMinutes" class="text-danger"></span>
                </div>
            </div>

            <div class="mb-3">
                <label asp-for="Description" class="form-label text-gold">
                    <i class="bi bi-pencil-square me-1"></i> Description
                </label>
                <textarea asp-for="Description" rows="4" class="form-control" placeholder="Brief description…"></textarea>
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>

            <div class="d-flex gap-3 justify-content-end">
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-check2-circle me-1"></i> Create
                </button>
                <a asp-action="Index" class="btn btn-outline-gold">
                    <i class="bi bi-x-circle me-1"></i> Cancel
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
